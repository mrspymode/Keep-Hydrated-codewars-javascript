//working every other place
const num = prompt('Enter a Time: ');
const b = 2;
const res = parseInt(num)/parseInt(b);
console.log(parseInt(res));
//working on codewors
Litres = time => Math.floor(time * 0.5)